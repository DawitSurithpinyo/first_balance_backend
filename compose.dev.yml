services:
  redis:
    image: redis:alpine3.22
    container_name: fb_redis
    environment:
      REDIS_DEV_USER: Punn
      REDIS_DEV_PASSWORD: mongodb
      REDIS_DEV_HOST: localhost
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/var/lib/redis/data

volumes:
  redis_data: